{
  "name": "doubak-export-adapters",
  "version": "0.1.0",
  "description": "豆备 (Doubak) 的对外导出适配器——canonical → NeoDB / Letterboxd / Goodreads。纯函数，不联网。",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "doubak-export": "./bin/export.js"
  },
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {},
  "devDependencies": {}
}
